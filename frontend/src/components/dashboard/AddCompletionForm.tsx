import { useState } from "react" 

interface AddCompletionFormProps { 
    onAddCompletion: (habit_id: string, date: number) => void 
}

const AddCompletionForm = ({ onAddCompletion }: AddCompletionFormProps) => { 

    return ( 
        <form>
            
        </form>
    )
}